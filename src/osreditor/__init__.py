"""osr-editor: a local GUI application for authoring osrlib adventure modules.

The package is the FastAPI backend of the editor: it holds the working document
as real osrlib model objects, applies typed edit operations, and serves the
built React frontend. See `docs/spec.md` for the architecture and contracts.
"""
